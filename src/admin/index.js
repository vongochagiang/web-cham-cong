import React from 'react';
import ReactDOM from 'react-dom/client';
import NhanVienTest from './NhanVienTest';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NhanVienTest />
  </React.StrictMode>
);
